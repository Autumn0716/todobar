## 1. macOS Project Setup

- [x] 1.1 Choose the smallest native macOS project structure that supports checked-in build and run commands.
- [x] 1.2 Add the macOS app target, entry point, and repository build/run workflow files.

## 2. App Model and Persistence

- [x] 2.1 Define Codable models for tasks, sections, custom lists, panel state, and settings.
- [x] 2.2 Seed default Chinese sections and sample data on first launch.
- [x] 2.3 Persist and restore tasks, collapsed state, panel state, and settings locally.

## 3. Native TodoBar UI

- [x] 3.1 Implement the left-edge handle and slide-out panel shell.
- [x] 3.2 Implement Chinese task sections, task rows, add controls, completion toggles, delete controls, and section collapse.
- [x] 3.3 Implement custom list creation and per-list task entry.
- [x] 3.4 Implement Chinese settings controls for theme, panel dimensions, handle position, task row sizing, motion, corner radius, and opacity.
- [x] 3.5 Upgrade the SwiftUI visual layer using native materials, SF Symbols, semantic colors, continuous corners, and spring motion.

## 4. Verification

- [x] 4.1 Run the checked-in build command and fix compiler errors.
- [x] 4.2 Run the checked-in launch command and verify the app opens as a native macOS app.
- [x] 4.3 Verify Chinese UI, task actions, custom lists, settings updates, and relaunch persistence.
