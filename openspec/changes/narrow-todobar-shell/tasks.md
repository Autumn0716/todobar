## 1. Visual Shell

- [x] 1.1 Remove the decorative background from the native SwiftUI root view.
- [x] 1.2 Configure the macOS window as clear/non-opaque.
- [x] 1.3 Size the app content around the TodoBar panel and handle.

## 2. App Icon

- [x] 2.1 Add a black-and-white TodoBar icon source/asset.
- [x] 2.2 Package the generated icon into `dist/TodoBar.app`.
- [x] 2.3 Reference the icon from the app bundle `Info.plist`.

## 3. Verification

- [x] 3.1 Run Swift tests.
- [x] 3.2 Run web tests/build to ensure the repository still passes existing checks.
- [x] 3.3 Run the app verification script and confirm launch succeeds.
